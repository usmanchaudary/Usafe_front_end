const createHeader = (heading) => {
  return `<div class="container">

    <div class="navbar mb-lg-4">
        <i class="fas fa-arrow-left navbar-back"></i>
        <span class="mb-lg-3">${heading}</span>
    </div>
    </div>`;
};
